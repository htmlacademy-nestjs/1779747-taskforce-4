import { sharedSharedPipes } from './shared-shared-pipes';

describe('sharedSharedPipes', () => {
  it('should work', () => {
    expect(sharedSharedPipes()).toEqual('shared-shared-pipes');
  });
});
